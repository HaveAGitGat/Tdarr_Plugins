// Eslint warning suppressed because it doesn't make any sense. The only occurrence is in this file!!!
// eslint-disable-next-line no-shadow
enum FileProperties {
  codec = 'codec',
  container = 'container',
  resolution = 'resolution',
}

export default FileProperties;
